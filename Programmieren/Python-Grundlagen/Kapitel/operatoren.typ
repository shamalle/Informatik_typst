#import "@preview/colorful-boxes:1.4.3": colorbox

= Operatoren

Die Operatoren bestimmen, was man mit den Variablen machen kann. Sie verknüpfen Variablen oder Werte und liefern ein Resultat.

#colorbox(
  title: "Aufgabe",
  radius: 2pt,
  width: auto,
)[
  + Sie haben folgendes ausgemessenes Zimmer, bei dem an den Wänden langsam die Farbe abblättert.
    #image("../Bilder/aufgabe_zimmer_1.png", width: 50%)
    Erstellen Sie in WebTigerPython passende Variablen und berechnen Sie die Gesamtfläche, die neu gestrichen werden muss.

    Sie können mit folgendem Code starten und dann weiter ausbauen:

    ```py
    laenge = 4
    hoehe = 2.5
    breite = ...
    wand_links = hoehe * breite
    ...
    gesamtflaeche = ...
    print(gesamtflaeche)
    ```
]
